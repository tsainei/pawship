
<%# <div class="match-container"> %>

  <h1 class="mt-3 mb-3 text-center">This is a match!</h1>

  <div class="card text-center mt-3 mb-3">
  <%# <div class="match-card"> %>
    <%= cl_image_tag(@swiped_dog.photo.key) %>
    <h3 class="mt-2"> <%= @swiped_dog.name %>, <%= @swiped_dog.age %></h3>
  </div>
  <p class="mt-3 text-center">Contact <%= @swiped_dog.name %>'s owner at: <a href = "mailto: kerrypang15@gmail.com"><%= @swiped_dog.user.email %></a></p>
  <%= link_to "Details", dog_path(@swiped_dog), class: "btn btn-gray" %>

  <%= link_to 'More Playdates', dogs_path, class: "btn btn-primary" %>


<%# </div> %>
